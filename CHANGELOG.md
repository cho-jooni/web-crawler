# 이 갈래에서 고친 것

[`byungjunjang/web-crawler`](https://github.com/byungjunjang/web-crawler) 를 기준으로,
동작을 바꾼 것만 적는다. 문구 다듬기는 적지 않는다.

---

## 안전 게이트 — 소프트블록 오탐

**증상.** 851바이트짜리 정상 JSON API 응답이 `blocked: challenge` 로 판정됐다.

```python
>>> detect_softblock(json.dumps({"items": [...20건...]}), status=200)
{'blocked': True, 'verdict': 'challenge',
 'signals': ['response too small: 851B < 3000B']}
```

**왜 문제인가.** `SKILL.md` Step 5.0 은 `blocked=True` 면 수집을 멈추고 **이음매 통지
게이트로 돌아가라**고 규정한다. 즉 차단이 전혀 없는 사이트에서 사용자에게 "우회할까요"
를 묻게 된다. 그런데 이 저장소가 1순위로 권하는 수집 경로가 바로 숨은 API(`plain_session`)
이고, API 응답은 3KB 를 넘지 않는 쪽이 보통이다. 가장 권장되는 경로가 가장 잘 오탐했다.

경고가 자주 틀리면 사람은 경고 자체를 무시한다 — PII 전화번호 패턴에서 이미 겪은 문제와
같은 축이다.

**고친 것.** 판정을 두 층으로 나눴다.

- **결정적 신호** (단독으로 차단): HTTP 401/402/403 · 챌린지 마커 · Akamai `_abck=~-1~`
- **보강 신호** (단독으로 차단하지 않음): 응답 크기. `selector_hit=True` 이거나 본문이
  내용 있는 JSON 으로 파싱되면 작아도 정상으로 본다. 빈 배열·빈 객체는 빈 셸과 구별되지
  않으므로 데이터로 치지 않는다.

`docstring` 이 "4단계 AND 검증" 이라고 적혀 있었지만 구현은 OR 였다. 문서
(`antibot-strategies.md`)와 함께 실제 동작에 맞췄다.

**테스트.** 이 함수에는 단위 테스트가 하나도 없었다(같은 파일의 PII 검사는 15개). 15개 추가.

---

## 산출물 — 엑셀 출력 3건

파이프라인의 마지막 단계다. 여기서 실패하면 오래 걸린 수집 결과가 통째로 날아간다.

### 1. 열이 조용히 사라졌다

`headers = list(data[0].keys())` — **첫 행의 키만** 헤더가 됐다. 뒤쪽 행에만 있는 필드는
경고 없이 엑셀에서 빠졌다. 스크래핑에서 이건 예외가 아니라 정상이다(할인가·옵션·품절표시).

```python
[{"상품명": "A", "가격": 100},
 {"상품명": "B", "가격": 200, "할인가": 150}]   # → '할인가' 열이 없는 엑셀
```

이 저장소가 스스로 적어 둔 원칙이 **"조용한 오답은 크래시보다 나쁘다"** 다. 모든 행의 키를
합집합으로(첫 등장 순서 유지) 모으도록 고쳤다.

### 2. 중첩 값에서 죽었다

`ValueError: Cannot convert ['a.jpg', 'b.jpg'] to Excel` — 이미지 목록·옵션처럼 중첩된 값이
들어오면 저장 단계에서 예외가 났다. 스칼라 리스트는 `", "` 로, 그 외에는 JSON 으로 편다.

### 3. 제어문자에서 죽었다

`IllegalCharacterError` — 스크랩한 본문에 제어문자가 섞이는 일은 흔하다. 제거한다.
셀 문자 상한(32,767)을 넘는 값도 잘라서 표시를 남긴다. 넘기면 크래시는 안 나지만
엑셀이 파일을 "읽을 수 없는 내용" 으로 판정한다 — 크래시 없이 산출물만 깨진다.

**테스트** 8개 추가. 비-dict 행이 섞여도 죽지 않게 했다.

---

## Windows — 안내 문구가 크래시로 바뀌었다

주 플랫폼이 Windows 인데(`setup.ps1` · PowerShell 안내) 기본 콘솔 인코딩은 cp949 다.
안내 문구에 cp949 에 없는 글자(`—` `⚠` `₩`)가 섞여 있어 그대로 `print` 하면
`UnicodeEncodeError` 로 죽었다.

- `sync_domain_list.py --check` — 문서가 시키는 검증 명령이 **성공 메시지에서** 죽었다
- `chrome_cdp.py` — Chrome 이 이미 떠 있을 때 나오는 안내가 `⚠` 에서 죽었다.
  무엇이 잘못됐는지 알려주는 문구 자체가 크래시로 바뀌는 자리다

`preflight.py` 와 `bootstrap.py` 에는 이미 인코딩 가드가 있었다. 없던 세 파일에 같은
관용구를 넣고, **정적 검사 테스트**로 고정했다 — cp949 로 인코딩 불가한 문자를 담은 채
`print` 하는 모듈은 가드를 갖고 있어야 한다. CI 가 ubuntu 뿐이라 이 계열은 실행으로는
잡히지 않는다.

---

## 미러 생성이 실패하면 반쯤 지워진 채 남았다

`sync_codex_mirror.build_mirror()` 는 `rmtree(dst)` 로 먼저 지우고 **그 자리에서** 다시
채웠다. 삭제나 복사가 중간에 실패하면 미러가 반쯤 지워진 상태로 남는다.

이 저장소를 OneDrive 폴더에서 작업하다 실제로 재현됐다:

```
PermissionError: [WinError 5] 액세스가 거부되었습니다:
  '...\.codex\skills\web-crawler\references'
→ 참조 문서 3건이 지워진 채 디렉터리 삭제만 실패. 미러가 깨진 상태로 남음
```

반쯤 지워진 미러는 그 다음 `--check` 에서 그냥 `STALE` 로 보고된다. 원인이 파일 잠금이었다는
사실은 어디에도 안 남고, 사람은 스킬 문서를 고친 적도 없는데 왜 stale 이냐고 헤맨다.

**고친 것.**
- 옆에 완성본(`.new`)을 만들어 두고 **마지막에 한 번만 교체**한다. 도중에 실패하면 기존
  미러가 그대로 살아 있다
- `rmtree` 에 재시도(최대 5회, 0.2s 씩 증가)와 읽기전용 속성 해제를 붙였다. Windows 에서
  OneDrive·백신·인덱서가 방금 쓴 파일 핸들을 잠깐 붙드는 것이 원인이라 대개 곧 풀린다.
  무한정 재시도하지는 않는다 — 진짜 실패는 실패로 올린다

**테스트** 7개 추가(이 모듈에는 테스트가 없었다). 실패 주입 시 이전 미러가 온전한지 확인한다.

---

## CI

- `actions/checkout@v4` → `v5`, `actions/setup-python@v5` → `v6`
  (Node 20 타깃이라 러너가 Node 24 로 강제 실행하며 deprecation 경고를 남겼다)
- **ubuntu + windows 매트릭스.** 주 플랫폼이 Windows 인데 ubuntu 에서만 돌렸다.
  위의 인코딩·파일잠금 계열이 CI 에서 한 번도 안 걸린 이유다
- **파이썬 3.10 · 3.11 · 3.13 매트릭스.** README·requirements 는 3.10 이상을 지원한다고
  말하는데 CI 는 3.11 하나만 돌렸다. 그 약속이 지켜지는지 아무도 검증하지 않고 있었다.
  실제로 이 갈래를 만들면서 3.12 전용 API(`shutil.rmtree(onexc=)`)를 썼다가 3.11 에서
  터졌고, 매트릭스가 없었다면 그대로 나갔을 것이다
- pip 캐시 추가 — `scrapling[fetchers]` 가 playwright·patchright 까지 끌고 와 무겁다
- 드리프트 검사를 별도 job 으로 분리(플랫폼 무관이라 한 번만 돌면 된다)
- `workflow_dispatch` 추가

---

## 테스트

| | 전 | 후 |
|---|---|---|
| 단위 테스트 | 415 | **461** |

신규 46건: 소프트블록 15 · 엑셀 8 · 콘솔 인코딩 11 · 미러 원자성 7 · 기타 5.
e2e 13건은 기존과 동일하게 제외(실제 사이트에 부하를 주지 않기 위해).
